import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: "#1a1a1aff",
    },
    secondary: {
      main: "#6299FF",
    },
  },
  typography: {
    fontFamily: "Segoe UI"
  }
});

export default theme;